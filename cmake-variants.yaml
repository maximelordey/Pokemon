buildType:
  default: debug 
  choices:
    debug:
      short: Debug
      long: Emit debug information, compiler optimisations are disabled
      buildType: Debug
      settings: 
        CMAKE_CXX_FLAGS: -std=c++11 -c -O0 -Wall -g
    release:
      short: Release
      long: Emit debug information, compiler optimisations are enabled
      buildType: Release
      settings: 
        CMAKE_CXX_FLAGS: -std=c++11 -c -O2 -Wall -g
    production:
      short: Production
      long: Omit debug information, compiler optimisations are enabled
      buildType: Production
      settings:
        CMAKE_CXX_FLAGS: -std=c++11 -O2

